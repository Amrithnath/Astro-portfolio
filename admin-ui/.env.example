PUBLIC_API_BASE_URL=http://127.0.0.1:8787

# Optional local-only debug header so the admin UI can hit the API
# before Cloudflare Access is wired in front of local development.
PUBLIC_ADMIN_DEBUG_EMAIL=arjun.amrith@gmail.com
